[profile.default]
src = 'contracts'
out = 'out'
libs = ['node_modules', 'lib']
optimizer = true
optimizer_runs = 200
gas_reports = ["*"]

[profile.gas]
test = 'test_gas'

[fuzz]
runs = 1000
max_test_rejects = 0
